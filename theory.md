# Билет 1. Следствия из аксиом евклидова пространства, неравенство Коши – Буняковского

## Формулировка

**Следствия из аксиом скалярного произведения:**

1. $( \bar{0}; \bar{x} ) = ( \bar{x}; \bar{0} ) = 0$
2. $( \bar{z}; \bar{x} + \bar{y} ) = ( \bar{z}; \bar{x} ) + ( \bar{z}; \bar{y} )$
3. $( \bar{x}; \alpha \bar{y} ) = \alpha \cdot ( \bar{x}; \bar{y} )$

**Теорема (неравенство Коши – Буняковского):**
Для любых векторов $\bar{x}, \bar{y} \in V$:
$$
|(\bar{x}; \bar{y})| \le \|\bar{x}\| \cdot \|\bar{y}\|
$$

## Доказательство (неравенства Коши – Буняковского)

Для любого $\alpha \in \mathbb{R}$ рассмотрим скалярный квадрат вектора $\bar{x} - \alpha\bar{y}$. По аксиоме 4 он неотрицателен:
$$
(\bar{x} - \alpha\bar{y}; \bar{x} - \alpha\bar{y}) \ge 0
$$

Раскрывая по аксиомам:
$$
(\bar{x} - \alpha\bar{y}; \bar{x} - \alpha\bar{y}) = (\bar{y}; \bar{y}) \cdot \alpha^2 - 2(\bar{x}; \bar{y}) \cdot \alpha + (\bar{x}; \bar{x}) \ge 0
$$

Обозначим $a = (\bar{y}; \bar{y})$, $b = -2(\bar{x}; \bar{y})$, $c = (\bar{x}; \bar{x})$. Получили квадратный трёхчлен относительно $\alpha$ с $a > 0$ (при $\bar{y} \ne \bar{0}$). Он неотрицателен при всех $\alpha$ тогда и только тогда, когда $\mathcal{D} \le 0$:
$$
\mathcal{D} = 4(\bar{x}; \bar{y})^2 - 4(\bar{y}; \bar{y}) \cdot (\bar{x}; \bar{x}) \le 0
$$

Отсюда $(\bar{x}; \bar{y})^2 \le (\bar{y}; \bar{y}) \cdot (\bar{x}; \bar{x})$, извлекая корень:
$$
|(\bar{x}; \bar{y})| \le \sqrt{(\bar{x}; \bar{x})} \cdot \sqrt{(\bar{y}; \bar{y})} = \|\bar{x}\| \cdot \|\bar{y}\|
$$

Случай $\bar{y} = \bar{0}$ тривиален: $0 \le 0$.

---

# Билет 2. Линейная зависимость или линейная независимость ортогональной системы векторов, процесс ортогонализации Грамма – Шмидта

## Формулировка

**Теорема 1 (линейная независимость ортогональной системы).** Если все векторы ортогональной системы ненулевые, то она линейно независима.

**Теорема 2 (процесс ортогонализации Грамма – Шмидта).** Пусть $\bar{f}_1, \dots, \bar{f}_m$ — ЛНС в евклидовом пространстве. Построим систему $\bar{e}_1, \dots, \bar{e}_m$:
$$
\bar{e}_1 = \bar{f}_1, \qquad
\bar{e}_k = \bar{f}_k - \sum_{i=1}^{k-1} \frac{(\bar{f}_k; \bar{e}_i)}{(\bar{e}_i; \bar{e}_i)} \cdot \bar{e}_i, \quad k = 2,\dots,m
$$
Тогда: 1) система $\bar{e}_1,\dots,\bar{e}_m$ линейно независима; 2) она ортогональна.

## Доказательство

**Теорема 1.** Пусть $\alpha_1 \bar{a}_1 + \dots + \alpha_m \bar{a}_m = \bar{0}$. Умножим скалярно на $\bar{a}_j$:
$$
\alpha_1 (\bar{a}_1; \bar{a}_j) + \dots + \alpha_j (\bar{a}_j; \bar{a}_j) + \dots + \alpha_m (\bar{a}_m; \bar{a}_j) = 0
$$
В силу ортогональности $(\bar{a}_i; \bar{a}_j) = 0$ при $i \ne j$, остаётся $\alpha_j (\bar{a}_j; \bar{a}_j) = 0$. Так как $\bar{a}_j \ne \bar{0}$, то $(\bar{a}_j; \bar{a}_j) > 0$, значит $\alpha_j = 0$. Аналогично для всех $j$. $\blacksquare$

**Теорема 2.**
*Линейная независимость:* каждый $\bar{e}_k$ выражается через $\bar{f}_1,\dots,\bar{f}_k$ с коэффициентом $1$ при $\bar{f}_k$. Матрица перехода — верхнетреугольная с единицами на диагонали, её определитель $= 1 \ne 0$, значит система $\bar{e}_1,\dots,\bar{e}_m$ линейно независима.

*Ортогональность (индукция по $k$):*
- База $k=2$: $(\bar{e}_2; \bar{e}_1) = (\bar{f}_2; \bar{e}_1) - \frac{(\bar{f}_2; \bar{e}_1)}{(\bar{e}_1; \bar{e}_1)}(\bar{e}_1; \bar{e}_1) = 0$.
- Шаг: пусть $\bar{e}_1,\dots,\bar{e}_{k-1}$ попарно ортогональны. Для $j < k$:
$$
(\bar{e}_k; \bar{e}_j) = (\bar{f}_k; \bar{e}_j) - \sum_{i=1}^{k-1} \frac{(\bar{f}_k; \bar{e}_i)}{(\bar{e}_i; \bar{e}_i)}(\bar{e}_i; \bar{e}_j)
$$
По предположению, $(\bar{e}_i; \bar{e}_j) = 0$ при $i \ne j$, остаётся только $i=j$:
$$
= (\bar{f}_k; \bar{e}_j) - \frac{(\bar{f}_k; \bar{e}_j)}{(\bar{e}_j; \bar{e}_j)}(\bar{e}_j; \bar{e}_j) = 0 \quad \blacksquare
$$

---

# Билет 3. Свойства ортонормированного базиса

## Формулировка

**Свойство 1 (существование ОНБ).** В любом конечномерном евклидовом пространстве существует ортонормированный базис.

**Свойство 2 (координаты в ОНБ).** Если Б $= (\bar{e}_1,\dots,\bar{e}_n)$ — ОНБ, то $[\bar{x}]_Б = ((\bar{x}; \bar{e}_1), \dots, (\bar{x}; \bar{e}_n))^T$.

**Свойство 3 (скалярное произведение в координатах).** Если Б — ОНБ, то $(\bar{x}; \bar{y}) = x_1 y_1 + \dots + x_n y_n$.

**Свойство 4 (матрица перехода между ОНБ).** Матрица перехода от одного ОНБ к другому ортогональна: $T^T = T^{-1}$.

## Доказательство

**Свойство 1.** Берём произвольный базис, применяем Gram–Schmidt (билет 2), получаем ортогональный базис, затем нормируем: $\bar{e}_i = \bar{g}_i / \|\bar{g}_i\|$. $\blacksquare$

**Свойство 2.** Разложим $\bar{x} = \sum x_i \bar{e}_i$. Умножая скалярно на $\bar{e}_j$, в силу ортонормированности $(\bar{e}_i; \bar{e}_j) = \delta_{ij}$ получаем $(\bar{x}; \bar{e}_j) = x_j$. $\blacksquare$

**Свойство 3.** $(\bar{x}; \bar{y}) = (\sum x_i \bar{e}_i; \sum y_j \bar{e}_j) = \sum_i \sum_j x_i y_j (\bar{e}_i; \bar{e}_j) = \sum_i x_i y_i$. $\blacksquare$

**Свойство 4.** $(T^T T)_{ij} = \sum_k t_{ki} t_{kj} = (\bar{e}'_i; \bar{e}'_j) = \delta_{ij} \implies T^T T = E \implies T^T = T^{-1}$. $\blacksquare$

---

# Билет 4. Определитель Грама

## Формулировка

**Теорема (об определителе Грама).** Пусть $V$ — $n$-мерное евклидово пространство, $S = (\bar{f}_1,\dots,\bar{f}_n)$ — система из $n$ векторов. Тогда:
1) Если $S$ линейно независима, то $|\Gamma_S| > 0$.
2) Если $S$ линейно зависима, то $|\Gamma_S| = 0$.

## Доказательство

Зафиксируем ОНБ Б $= (\bar{e}_1,\dots,\bar{e}_n)$. Разложим $\bar{f}_j = \sum_{i=1}^n a_{ij} \bar{e}_i$. Составим матрицу $A = (a_{ij})$.

Вычислим $(\bar{f}_i; \bar{f}_j) = \sum_k a_{ki} a_{kj} = (A^T A)_{ij}$. Следовательно, $\Gamma_S = A^T A$.

Тогда $|\Gamma_S| = |A^T A| = |A^T| \cdot |A| = |A|^2 \ge 0$.

- Если $S$ ЛНС, то столбцы $A$ ЛНС $\implies |A| \ne 0 \implies |\Gamma_S| = |A|^2 > 0$.
- Если $S$ ЛЗС, то $|A| = 0 \implies |\Gamma_S| = 0$. $\blacksquare$

---

# Билет 5. Свойства ортогонального дополнения

## Формулировка

Пусть $L$ — подпространство конечномерного евклидова пространства $V$. Тогда:
1) $\forall \bar{x} \in V\ \exists!\, \bar{y} \in L,\ \exists!\, \bar{z} \in L^\perp: \bar{x} = \bar{y} + \bar{z}$ (разложение единственно).
2) $\dim(L^\perp) + \dim(L) = \dim(V)$.

## Доказательство

**Свойство 1 (существование).** Выберем в $L$ ОНБ $(\bar{e}_1,\dots,\bar{e}_m)$. Для $\bar{x} \in V$ определим проекцию: $\bar{y} = \sum_{i=1}^m (\bar{x}; \bar{e}_i) \bar{e}_i \in L$, и $\bar{z} = \bar{x} - \bar{y}$. Тогда $(\bar{z}; \bar{e}_j) = (\bar{x}; \bar{e}_j) - (\bar{y}; \bar{e}_j) = 0$, значит $\bar{z} \in L^\perp$.

*Единственность.* Если $\bar{x} = \bar{y}_1 + \bar{z}_1 = \bar{y}_2 + \bar{z}_2$, то $\bar{y}_1 - \bar{y}_2 = \bar{z}_2 - \bar{z}_1$. Этот вектор лежит и в $L$, и в $L^\perp$, значит ортогонален сам себе: $(\bar{w}; \bar{w}) = 0 \implies \bar{w} = \bar{0}$. Отсюда $\bar{y}_1 = \bar{y}_2$, $\bar{z}_1 = \bar{z}_2$.

**Свойство 2.** Из свойства 1 следует $V = L \oplus L^\perp$. Объединение базисов $L$ и $L^\perp$ даёт базис $V$, значит $\dim(V) = \dim(L) + \dim(L^\perp)$. $\blacksquare$

---

# Билет 6. Критерии линейности оператора. Теорема о линейности суммы операторов, произведения оператора и числа, а также произведения операторов

## Формулировка

**Теорема 1 (критерий линейности).**
$$
\hat{A} : V \to V \text{ — линейный оператор} \iff \forall \bar{x},\bar{y} \in V\ \forall \alpha,\beta \in P:\ \hat{A}(\alpha\bar{x} + \beta\bar{y}) = \alpha\hat{A}(\bar{x}) + \beta\hat{A}(\bar{y})
$$

**Теорема 2 (о линейности операций).** Если $\hat{A}$ и $\hat{B}$ — линейные операторы, то $\hat{A} + \hat{B}$, $\alpha\hat{A}$ и $\hat{A}\hat{B}$ — также линейные операторы.

## Доказательство

**Теорема 1.**
*Необходимость ($\Rightarrow$):* По аддитивности $\hat{A}(\alpha\bar{x} + \beta\bar{y}) = \hat{A}(\alpha\bar{x}) + \hat{A}(\beta\bar{y})$, по однородности $= \alpha\hat{A}(\bar{x}) + \beta\hat{A}(\bar{y})$.

*Достаточность ($\Leftarrow$):* Подставляя $\alpha = \beta = 1$, получаем аддитивность: $\hat{A}(\bar{x} + \bar{y}) = \hat{A}(\bar{x}) + \hat{A}(\bar{y})$. Подставляя $\beta = 0$, получаем однородность: $\hat{A}(\alpha\bar{x}) = \alpha\hat{A}(\bar{x})$. $\blacksquare$

**Теорема 2.** Проверяется по критерию линейности:
- *Сумма:* $(\hat{A} + \hat{B})(\alpha\bar{x} + \beta\bar{y}) = \hat{A}(\alpha\bar{x} + \beta\bar{y}) + \hat{B}(\alpha\bar{x} + \beta\bar{y}) = \alpha(\hat{A}+\hat{B})(\bar{x}) + \beta(\hat{A}+\hat{B})(\bar{y})$.
- *Умножение на число:* $(\gamma\hat{A})(\alpha\bar{x} + \beta\bar{y}) = \gamma\hat{A}(\alpha\bar{x} + \beta\bar{y}) = \alpha(\gamma\hat{A})(\bar{x}) + \beta(\gamma\hat{A})(\bar{y})$.
- *Композиция:* $(\hat{A}\hat{B})(\alpha\bar{x} + \beta\bar{y}) = \hat{A}(\hat{B}(\alpha\bar{x} + \beta\bar{y})) = \hat{A}(\alpha\hat{B}(\bar{x}) + \beta\hat{B}(\bar{y})) = \alpha(\hat{A}\hat{B})(\bar{x}) + \beta(\hat{A}\hat{B})(\bar{y})$. $\blacksquare$

---

# Билет 7. Матрица оператора. Теорема о связи координат образа и прообраза. Теорема о связи матриц одного и того же оператора в разных базисах

## Формулировка

**Теорема 1 (о связи координат образа и прообраза).** Пусть $\hat{A}$ — ЛО, Б — базис в $V$. Тогда $\forall \bar{x} \in V$:
$$
[\hat{A}(\bar{x})]_Б = [\hat{A}]_Б \cdot [\bar{x}]_Б
$$

**Теорема 2 (о связи матриц оператора в разных базисах).** $\forall$ базисов Б, Б' и $\forall$ ЛО $\hat{A}$:
$$
[\hat{A}]_{Б'} = T_{Б \to Б'} \cdot [\hat{A}]_Б \cdot T_{Б' \to Б}
$$

## Доказательство

**Теорема 1.** Разложим $\bar{x} = \sum x_j \bar{e}_j$. По линейности:
$$
\hat{A}(\bar{x}) = \sum_{j=1}^n x_j \hat{A}(\bar{e}_j) = \sum_{j=1}^n x_j \sum_{i=1}^n a_{ij} \bar{e}_i = \sum_{i=1}^n \left(\sum_{j=1}^n a_{ij} x_j\right) \bar{e}_i
$$
Следовательно, $[\hat{A}(\bar{x})]_Б = [\hat{A}]_Б \cdot [\bar{x}]_Б$. $\blacksquare$

**Теорема 2.** Из теоремы 1 и связи координат $[\bar{x}]_Б = T_{Б' \to Б} \cdot [\bar{x}]_{Б'}$:
$$
[\hat{A}(\bar{x})]_Б = [\hat{A}]_Б \cdot T_{Б' \to Б} \cdot [\bar{x}]_{Б'} = T_{Б' \to Б} \cdot [\hat{A}]_{Б'} \cdot [\bar{x}]_{Б'}
$$
Отсюда $T_{Б' \to Б} \cdot [\hat{A}]_{Б'} = [\hat{A}]_Б \cdot T_{Б' \to Б}$, и:
$$
[\hat{A}]_{Б'} = T_{Б' \to Б}^{-1} \cdot [\hat{A}]_Б \cdot T_{Б' \to Б} = T_{Б \to Б'} \cdot [\hat{A}]_Б \cdot T_{Б' \to Б} \quad \blacksquare
$$

---

# Билет 8. Теорема о связи алгебры операторов с алгеброй их матриц

## Формулировка

Пусть $\hat{A}$ и $\hat{B}$ — ЛО в конечномерном $V$, Б — базис $V$. Тогда:
1) $[\hat{A} + \hat{B}]_Б = [\hat{A}]_Б + [\hat{B}]_Б$
2) $[\hat{A} \hat{B}]_Б = [\hat{A}]_Б \cdot [\hat{B}]_Б$
3) $\forall \alpha \in P: [\alpha \hat{A}]_Б = \alpha [\hat{A}]_Б$

## Доказательство

**1)** $[(\hat{A} + \hat{B})(\bar{x})]_Б = [\hat{A} + \hat{B}]_Б \cdot [\bar{x}]_Б$. С другой стороны, $[(\hat{A} + \hat{B})(\bar{x})]_Б = [\hat{A}(\bar{x}) + \hat{B}(\bar{x})]_Б = [\hat{A}]_Б \cdot [\bar{x}]_Б + [\hat{B}]_Б \cdot [\bar{x}]_Б = ([\hat{A}]_Б + [\hat{B}]_Б) \cdot [\bar{x}]_Б$. Ввиду произвольности $\bar{x}$, матрицы равны.

**2)** $[(\hat{A}\hat{B})(\bar{x})]_Б = [\hat{A}\hat{B}]_Б \cdot [\bar{x}]_Б = [\hat{A}(\hat{B}(\bar{x}))]_Б = [\hat{A}]_Б \cdot [\hat{B}(\bar{x})]_Б = [\hat{A}]_Б \cdot [\hat{B}]_Б \cdot [\bar{x}]_Б$. Отсюда $[\hat{A}\hat{B}]_Б = [\hat{A}]_Б \cdot [\hat{B}]_Б$.

**3)** $[(\alpha\hat{A})(\bar{x})]_Б = [\alpha\hat{A}]_Б \cdot [\bar{x}]_Б = [\alpha \cdot \hat{A}(\bar{x})]_Б = \alpha \cdot [\hat{A}]_Б \cdot [\bar{x}]_Б$. Отсюда $[\alpha\hat{A}]_Б = \alpha [\hat{A}]_Б$. $\blacksquare$

---

# Билет 9. Ядро и область значений как подпространства. Теорема о связи ранга и дефекта линейного оператора

## Формулировка

**Теорема 1.** $\operatorname{Ker}(\hat{A}) \le V$, $\operatorname{Im}(\hat{A}) \le V$.

**Теорема 2 (о ранге и дефекте).** $\operatorname{Rg}(\hat{A}) + d(\hat{A}) = \dim(V)$.

## Доказательство

**Теорема 1.**
*Ядро:* Если $\bar{x},\bar{y} \in \operatorname{Ker}(\hat{A})$, то $\hat{A}(\bar{x}+\bar{y}) = \hat{A}(\bar{x}) + \hat{A}(\bar{y}) = \bar{0} \implies \bar{x}+\bar{y} \in \operatorname{Ker}(\hat{A})$. Если $\bar{x} \in \operatorname{Ker}(\hat{A})$, $\alpha \in P$, то $\hat{A}(\alpha\bar{x}) = \alpha\hat{A}(\bar{x}) = \bar{0} \implies \alpha\bar{x} \in \operatorname{Ker}(\hat{A})$.

*Образ:* Если $\bar{y}_1,\bar{y}_2 \in \operatorname{Im}(\hat{A})$, то $\exists \bar{x}_1,\bar{x}_2: \hat{A}(\bar{x}_1)=\bar{y}_1,\ \hat{A}(\bar{x}_2)=\bar{y}_2$. Тогда $\bar{y}_1+\bar{y}_2 = \hat{A}(\bar{x}_1+\bar{x}_2) \in \operatorname{Im}(\hat{A})$. Аналогично для умножения на число. $\blacksquare$

**Теорема 2.** Пусть $\dim(V)=n$, $d(\hat{A})=d$. Выберем базис ядра $(\bar{e}_1,\dots,\bar{e}_d)$, дополним до базиса $V$ векторами $\bar{e}_{d+1},\dots,\bar{e}_n$.

Любой $\bar{y} \in \operatorname{Im}(\hat{A})$: $\bar{y} = \hat{A}(\bar{x}) = \sum_{j=d+1}^n \alpha_j \hat{A}(\bar{e}_j)$, так как $\hat{A}(\bar{e}_1)=\dots=\hat{A}(\bar{e}_d)=\bar{0}$. Покажем, что $\hat{A}(\bar{e}_{d+1}),\dots,\hat{A}(\bar{e}_n)$ ЛНС: если $\sum \beta_j \hat{A}(\bar{e}_j)=\bar{0}$, то $\hat{A}(\sum \beta_j \bar{e}_j)=\bar{0}$, значит $\sum \beta_j \bar{e}_j \in \operatorname{Ker}(\hat{A})$, и его можно разложить по базису ядра. Ввиду ЛНС полного базиса, все $\beta_j=0$. Следовательно, $\operatorname{Rg}(\hat{A})=n-d$. $\blacksquare$

---

# Билет 10. Свойства характеристического многочлена

## Формулировка

**Теорема 1 (об инвариантности ХМ).** Характеристический многочлен конечномерного ЛО $\hat{A}$ имеет один и тот же вид во всех базисах.

**Теорема 2 (связь СЗ с корнями ХМ).** $\lambda \in P$ является СЗ ЛО $\hat{A} \iff \lambda$ — корень характеристического многочлена ЛО $\hat{A}$.

## Доказательство

**Теорема 1.**
$$
[\hat{A}]_{Б'} - \lambda E = T_{Б'\to Б} \cdot [\hat{A}]_Б \cdot T_{Б\to Б'} - \lambda T_{Б'\to Б} \cdot T_{Б\to Б'} = T_{Б'\to Б} \cdot ([\hat{A}]_Б - \lambda E) \cdot T_{Б\to Б'}
$$
Берём определитель:
$$
|[\hat{A}]_{Б'} - \lambda E| = |T_{Б'\to Б}| \cdot |[\hat{A}]_Б - \lambda E| \cdot |T_{Б\to Б'}| = |[\hat{A}]_Б - \lambda E|
$$
так как $|T_{Б'\to Б}| \cdot |T_{Б\to Б'}| = 1$. $\blacksquare$

**Теорема 2.** $\lambda$ — СЗ $\iff \exists \bar{x} \ne \bar{0}: \hat{A}(\bar{x}) = \lambda\bar{x} \iff \exists X \ne O: ([\hat{A}]_Б - \lambda E)X = O \iff |[\hat{A}]_Б - \lambda E| = 0$. $\blacksquare$

---

# Билет 11. Свойства собственных значений и собственных векторов: диагонализируемость матрицы оператора, количество линейно независимых собственных векторов

## Формулировка

**Утверждение 1.** Базис Б состоит из собственных векторов оператора $\hat{A}$ тогда и только тогда, когда матрица $[\hat{A}]_Б$ диагональна, причём на диагонали стоят соответствующие собственные значения.

**Утверждение 2.** Количество линейно независимых собственных векторов, соответствующих данному собственному значению, не превосходит кратности этого собственного значения как корня характеристического уравнения.

## Доказательство

**Утверждение 1.**
*Необходимость ($\Rightarrow$):* Если $\hat{A}(\bar{e}_j) = \lambda_j \bar{e}_j$, то $j$-й столбец матрицы $= (0,\dots,0,\lambda_j,0,\dots,0)^T$, значит матрица диагональна.
*Достаточность ($\Leftarrow$):* Если матрица диагональна с $\lambda_j$ на диагонали, то $\hat{A}(\bar{e}_j) = \lambda_j \bar{e}_j$, т.е. каждый базисный вектор — собственный. $\blacksquare$

**Утверждение 2.** Пусть $\lambda_0$ — корень кратности $k$. Предположим, есть $m = k+1$ ЛНС собственных векторов для $\lambda_0$. Дополним их до базиса $V$. В этом базисе левый верхний блок $(k+1)\times(k+1)$ матрицы $= \lambda_0 E_{k+1}$, левый нижний — нулевой. Тогда характеристический многочлен содержит множитель $(\lambda - \lambda_0)^{k+1}$, противоречие с кратностью $k$. $\blacksquare$

---

# Билет 12. Собственные векторы, соответствующие различным собственным значениям

## Формулировка

**Теорема.** Пусть $\bar{e}_{11},\dots,\bar{e}_{1n_1}$ — ЛНС собственных векторов для $\lambda_1$; $\dots$; $\bar{e}_{k1},\dots,\bar{e}_{kn_k}$ — ЛНС собственных векторов для $\lambda_k$, причём $\lambda_1,\dots,\lambda_k$ попарно различны. Тогда объединённая система всех этих векторов линейно независима.

## Доказательство

Индукция по $k$.

*База ($k=1$):* тривиально.

*Шаг:* предположим, верно для $k$. Докажем для $k+1$. Рассмотрим ЛК, равную нулю:
$$
\sum_{i=1}^{n_1} \alpha_{1i} \bar{e}_{1i} + \dots + \sum_{i=1}^{n_{k+1}} \alpha_{k+1,i} \bar{e}_{k+1,i} = \bar{0} \qquad (*)
$$

1. Применим $\hat{A}$:
$$
\lambda_1\sum\alpha_{1i}\bar{e}_{1i} + \dots + \lambda_{k+1}\sum\alpha_{k+1,i}\bar{e}_{k+1,i} = \bar{0} \qquad (1)
$$

2. Умножим $(*)$ на $\lambda_{k+1}$:
$$
\lambda_{k+1}\sum\alpha_{1i}\bar{e}_{1i} + \dots + \lambda_{k+1}\sum\alpha_{k+1,i}\bar{e}_{k+1,i} = \bar{0} \qquad (2)
$$

3. Вычтем (1) из (2):
$$
(\lambda_{k+1}-\lambda_1)\sum\alpha_{1i}\bar{e}_{1i} + \dots + (\lambda_{k+1}-\lambda_k)\sum\alpha_{ki}\bar{e}_{ki} = \bar{0}
$$

4. По предположению индукции, векторы $\bar{e}_{1i},\dots,\bar{e}_{ki}$ ЛНС, значит все коэффициенты — нули. Так как $\lambda_{k+1} \ne \lambda_j$, то $\alpha_{ji}=0$ для $j=1,\dots,k$.

5. Подставляя в $(*)$, получаем $\sum\alpha_{k+1,i}\bar{e}_{k+1,i} = \bar{0}$, откуда в силу ЛНС $\alpha_{k+1,i}=0$. $\blacksquare$

---


# Билет 13. Свойства самосопряжённого оператора (матрица СО в ОНБ, корни ХУ СО)

## Формулировка

1. Матрица самосопряжённого оператора в ОНБ — симметрична.
2. Корни характеристического уравнения самосопряжённого ЛО — вещественные.

## Доказательство

**1.** Пусть Б $= (\bar{e}_1,\dots,\bar{e}_n)$ — ОНБ, $\hat{A}$ — самосопряжён. $a_{ij} = (\hat{A}(\bar{e}_j); \bar{e}_i)$. В силу самосопряжённости:
$$
a_{ij} = (\hat{A}(\bar{e}_j); \bar{e}_i) = (\bar{e}_j; \hat{A}(\bar{e}_i)) = (\hat{A}(\bar{e}_i); \bar{e}_j) = a_{ji}
$$
Значит $[\hat{A}]_Б$ симметрична.

**2.** Пусть $A = [\hat{A}]_Б$, $X$ — СВ-столбец для СЗ $\lambda$: $A X = \lambda X$. Тогда:
$$
X^T A X = \lambda X^T X = \lambda (|x_1|^2 + \dots + |x_n|^2)
$$
С другой стороны, $\overline{X^T A X} = \overline{\lambda} (|x_1|^2 + \dots + |x_n|^2)$. Так как $A$ симметрична и вещественна, $\overline{X^T A X} = X^T A \overline{X} = X^T \overline{\lambda} \overline{X} = \overline{\lambda} (|x_1|^2 + \dots + |x_n|^2)$.

Приравнивая: $\lambda = \overline{\lambda} \implies \lambda \in \mathbb{R}$. $\blacksquare$

---

# Билет 14. Свойства самосопряжённого оператора: собственные векторы для различных СЗ; существование ОНБ из собственных векторов

## Формулировка

1. Собственные векторы самосопряжённого ЛО, соответствующие различным собственным значениям, ортогональны.
2. У самосопряжённого оператора существует ОНБ из собственных векторов.

## Доказательство

**1.** Пусть $\hat{A}(\bar{e}_1) = \lambda_1 \bar{e}_1$, $\hat{A}(\bar{e}_2) = \lambda_2 \bar{e}_2$, $\lambda_1 \ne \lambda_2$:
$$
(\hat{A}(\bar{e}_1); \bar{e}_2) = \lambda_1(\bar{e}_1; \bar{e}_2) = (\bar{e}_1; \hat{A}(\bar{e}_2)) = \lambda_2(\bar{e}_1; \bar{e}_2)
$$
Отсюда $(\lambda_1 - \lambda_2)(\bar{e}_1; \bar{e}_2) = 0 \implies (\bar{e}_1; \bar{e}_2) = 0$. $\blacksquare$

**2.** Индукция по $n = \dim(V)$.
- $n=1$: любой ненулевой вектор — СВ, нормируем.
- Пусть верно для $n$, докажем для $n+1$. У ХУ есть вещественный корень $\lambda_1$ (по билету 13). Берём СВ $\bar{e}'_1$, нормируем: $\bar{e}_1 = \bar{e}'_1 / \|\bar{e}'_1\|$.
Покажем, что $\langle \bar{e}_1 \rangle^\perp$ инвариантно относительно $\hat{A}$: если $\bar{x} \perp \bar{e}_1$, то $(\hat{A}(\bar{x}); \bar{e}_1) = (\bar{x}; \hat{A}(\bar{e}_1)) = \lambda_1(\bar{x}; \bar{e}_1) = 0$, значит $\hat{A}(\bar{x}) \perp \bar{e}_1$.
$\dim\langle \bar{e}_1 \rangle^\perp = n$, на нём $\hat{A}$ тоже самосопряжён. По предположению индукции, $\exists$ ОНБ из СВ $(\bar{e}_2,\dots,\bar{e}_{n+1})$ в $\langle \bar{e}_1 \rangle^\perp$. Добавляя $\bar{e}_1$, получаем ОНБ $V$ из СВ. $\blacksquare$

---

# Билет 15. ДУ с разделяющимися переменными и ДУ-I, сводящиеся к ним

## Формулировка

**ДУ с разделяющимися переменными:**
$$
\varphi_1(x)\psi_1(y)\,dx + \varphi_2(x)\psi_2(y)\,dy = 0
$$

**Однородные ДУ-I:** $M(x,y)\,dx + N(x,y)\,dy = 0$, где $M,N$ — однородные одной степени. Сводятся к разделяющимся подстановкой $y = zx$.

**Линейные ДУ-I:** $y' + a(x)y = f(x)$. **ДУ Бернулли:** $y' + a(x)y = f(x)y^\alpha$ ($\alpha \ne 0,1$). Сводятся к разделяющимся подстановкой $y = e^{-\int a(x)\,dx} \cdot V$.

## Доказательство

**Разделяющиеся переменные.** В прямоугольнике $\Pi$, где $\varphi_2(x) \ne 0$ и $\psi_1(y) \ne 0$, делим на $\varphi_2(x)\psi_1(y)$:
$$
\frac{\varphi_1(x)}{\varphi_2(x)}\,dx + \frac{\psi_2(y)}{\psi_1(y)}\,dy = 0
$$
Обозначим $\Phi(x) = \int \frac{\varphi_1}{\varphi_2}\,dx$, $\Psi(y) = \int \frac{\psi_2}{\psi_1}\,dy$. Тогда $\Phi'(x) + (\Psi(y(x)))' = 0$, откуда $\Phi(x) + \Psi(y) = C$. Кроме того, решениями являются $y = y_l$ (корни $\psi_1$) и $x = x_k$ (корни $\varphi_2$).

**Однородные ДУ.** Подстановка $y = zx$, $dy = z\,dx + x\,dz$:
$$
M(x, zx)\,dx + N(x, zx)(z\,dx + x\,dz) = 0
$$
$$
x^k (M(1,z) + z N(1,z))\,dx + x^{k+1} N(1,z)\,dz = 0
$$
Получаем уравнение с разделяющимися переменными.

**Линейные ДУ и Бернулли.** Подстановка $y = e^{-\int a\,dx} \cdot V$:
$$
e^{-\int a\,dx}\,dV - f(x) e^{-\alpha\int a\,dx} V^\alpha\,dx = 0
$$
— уравнение с разделяющимися переменными.

---

# Билет 16. Методы понижения порядка ДУ-II

## Формулировка

Четыре типа ДУ-II, допускающих понижение порядка:

1. **Не содержит явно $x$:** $F(y, y', y'') = 0$. Подстановка $y' = z(y)$.
2. **Не содержит явно $y$:** $F(x, y', y'') = 0$. Подстановка $y' = z(x)$.
3. **Полная производная от ДУ-I:** Если $F'_x + F'_y \cdot y' + F'_z \cdot y'' = 0$, то $F(x, y, y') = C$.
4. **Однородное относительно $y, y', y''$:** $F(x, ty, ty', ty'') = t^k F$. Подстановка $y' = z(x) \cdot y$.

## Доказательство

**1.** $y' = z(y) \implies y'' = z' \cdot z$. Получаем $F(y, z, z') = 0$ — ДУ-I.

**2.** $y' = z(x) \implies y'' = z'$. Получаем $F(x, z, z') = 0$ — ДУ-I.

**3.** Если $(F(x, y(x), y'(x)))' = 0$, то $F(x, y, y') = C$ — ДУ-I.

**4.** $y' = zy \implies y'' = (z' + z^2)y$. Подставляем: $y^k F(x, 1, z, z' + z^2) = 0 \implies F(x, 1, z, z' + z^2) = 0$ — ДУ-I.

---

# Билет 17. Вронскиан системы функций: условие ЛЗ системы функций, пример с неоднозначностью

## Формулировка

**Теорема.** Если $y_1(x),\dots,y_k(x) \in C^{(k-1)}(I)$ — ЛЗС, то $W[y_1,\dots,y_k](x) \equiv 0$, $x \in I$.

Обратное утверждение **неверно** (пример ниже).

## Доказательство

Если $y_1,\dots,y_k$ линейно зависимы, то $\exists$ ненулевой набор $(\alpha_1,\dots,\alpha_k)$:
$$
\alpha_1 y_1(x) + \dots + \alpha_k y_k(x) \equiv 0
$$
Дифференцируя это тождество $(k-1)$ раз, получаем систему:
$$
\alpha_1 y_1^{(j)}(x) + \dots + \alpha_k y_k^{(j)}(x) \equiv 0, \quad j = 0,\dots,k-1
$$
Следовательно, в каждой точке $x \in I$ столбцы вронскиана линейно зависимы $\implies W \equiv 0$. $\blacksquare$

**Пример (опровергающий обратное).**
$$
y_1(x) = \begin{cases} (x-1)^2, & x \in (0;1] \\ 0, & x \in (1;2) \end{cases}, \qquad
y_2(x) = \begin{cases} 0, & x \in (0;1] \\ (x-1)^2, & x \in (1;2) \end{cases}
$$
Обе функции $C^{(1)}(0;2)$. Они ЛНС (если $\alpha_1 y_1 + \alpha_2 y_2 \equiv 0$, то на $(0;1]$ получаем $\alpha_1=0$, на $(1;2)$ — $\alpha_2=0$). Однако $W[y_1,y_2](x) \equiv 0$ на $(0;2)$.

Вывод: $W \equiv 0 \;\not\Rightarrow\;$ ЛЗС, но ЛЗС $\Rightarrow W \equiv 0$.

---

# Билет 18. Принцип суперпозиции для ЛДУ. Структура общего решения ЛНДУ

## Формулировка

**Теорема 1 (принцип суперпозиции).** Если $y_i(x)$ — решение ЛДУ с правой частью $f_i(x)$ ($i=1,\dots,k$), то $\forall \alpha_1,\dots,\alpha_k$:
$$
y = \alpha_1 y_1(x) + \dots + \alpha_k y_k(x)
$$
— решение ЛДУ с правой частью $\alpha_1 f_1(x) + \dots + \alpha_k f_k(x)$.

**Теорема 2 (структура общего решения ЛНДУ).** Пусть $y_1(x)$ — частное решение НЛДУ. Тогда:
1) $\forall$ решения $y_0(x)$ ОЛДУ, $y = y_0(x) + y_1(x)$ — решение НЛДУ.
2) $\forall$ решения $y_2(x)$ НЛДУ $\exists$ решение $y_0(x)$ ОЛДУ: $y_2(x) = y_0(x) + y_1(x)$.

Общее решение ЛНДУ: $y_{\text{о.н.}}(x) = y_{\text{о.о.}}(x) + y_{\text{ч.н.}}(x)$.

## Доказательство

**Теорема 1 (для $n=2$).** Линейность дифференцирования и группировка слагаемых дают:
$$
(\sum \alpha_i y_i)'' + a_1 (\sum \alpha_i y_i)' + a_0 (\sum \alpha_i y_i) = \sum \alpha_i (y_i'' + a_1 y_i' + a_0 y_i) = \sum \alpha_i f_i
$$

**Теорема 2.**
1) Если $y_0$ — решение ОЛДУ, $y_1$ — НЛДУ, то $(y_0+y_1)'' + a_1(y_0+y_1)' + a_0(y_0+y_1) = 0 + f = f$.
2) Если $y_1$, $y_2$ — два решения НЛДУ, то их разность $y_0 = y_2 - y_1$ удовлетворяет ОЛДУ, значит $y_2 = y_0 + y_1$. $\blacksquare$

---

# Билет 19. Принцип суперпозиции для ОЛДУ. Условие ЛН $n$ решений ОЛДУ

## Формулировка

**Теорема 1 (принцип суперпозиции для ОЛДУ).** Если $y_1,\dots,y_k$ — решения ОЛДУ, то $y = C_1 y_1 + \dots + C_k y_k$ — тоже решение.

**Теорема 2 (критерий ЛНС $n$ решений ОЛДУ $n$-го порядка).** Пусть $y_1,\dots,y_n$ — решения ОЛДУ $n$-го порядка. Тогда $y_1,\dots,y_n$ — ЛНС $\iff \forall x \in I: W[y_1,\dots,y_n](x) \ne 0$.

## Доказательство

**Теорема 1.** Следует из принципа суперпозиции для ЛДУ при $f_1 \equiv \dots \equiv f_k \equiv 0$. $\blacksquare$

**Теорема 2 (для $n=2$).**
$(\Rightarrow)$: от противного. Пусть $y_1,y_2$ ЛНС, но $\exists x_0: W(x_0)=0$. Тогда столбцы $W(x_0)$ ЛЗ: $\exists (\alpha_1,\alpha_2) \ne (0,0)$:
$$
\alpha_1 y_1(x_0) + \alpha_2 y_2(x_0) = 0,\quad \alpha_1 y_1'(x_0) + \alpha_2 y_2'(x_0) = 0
$$
Функция $Z(x) = \alpha_1 y_1(x) + \alpha_2 y_2(x)$ — решение ОЛДУ с нулевыми НУ. По теореме единственности, $Z(x) \equiv 0$, значит $y_1,y_2$ ЛЗ — противоречие.

$(\Leftarrow)$: следует из теоремы билета 17 (если бы были ЛЗ, то $W \equiv 0$). $\blacksquare$

---

# Билет 20. Существование ФСР у ОЛДУ. Структура общего решения ОЛДУ

## Формулировка

**Теорема 1 (о существовании ФСР).** У любого ОЛДУ (с непрерывными коэффициентами) существует ФСР.

**Теорема 2 (структура общего решения ОЛДУ).** Пусть $y_1,\dots,y_n$ — ФСР ОЛДУ $n$-го порядка. Тогда:
1) $\forall C_1,\dots,C_n$: $y = C_1 y_1 + \dots + C_n y_n$ — решение.
2) $\forall$ решения $y_0(x)$ $\exists C_1,\dots,C_n$: $y_0(x) \equiv C_1 y_1(x) + \dots + C_n y_n(x)$.

Общее решение: $y_{\text{о.о.}}(x) = C_1 y_1(x) + \dots + C_n y_n(x)$, где $y_i$ — ФСР, $C_i$ — произвольные постоянные.

## Доказательство

**Теорема 1 (для $n=2$).** Выберем $x_0 \in I$. Решим две задачи Коши:
$$
\begin{cases} y'' + a_1 y' + a_0 y = 0 \\ y(x_0)=1,\ y'(x_0)=0 \end{cases} \implies y_1(x)
$$
$$
\begin{cases} y'' + a_1 y' + a_0 y = 0 \\ y(x_0)=0,\ y'(x_0)=1 \end{cases} \implies y_2(x)
$$
Тогда $W[y_1,y_2](x_0) = \begin{vmatrix} 1 & 0 \\ 0 & 1 \end{vmatrix} = 1 \ne 0 \implies y_1,y_2$ — ФСР. $\blacksquare$

**Теорема 2 (для $n=2$).**
1) Следует из принципа суперпозиции для ОЛДУ.

2) Для $x_0 \in I$ решаем СЛУ:
$$
\begin{cases} y_1(x_0)C_1 + y_2(x_0)C_2 = y_0(x_0) \\ y_1'(x_0)C_1 + y_2'(x_0)C_2 = y_0'(x_0) \end{cases}
$$
Так как $W(x_0) \ne 0$, $\exists!$ решение $(C_1,C_2)$. Функция $Z(x)=C_1 y_1(x)+C_2 y_2(x)$ — решение той же задачи Коши, что и $y_0(x)$. По единственности, $y_0(x) \equiv Z(x)$. $\blacksquare$

---

# Билет 21. ФСР ОЛДУ 2-го порядка с постоянными коэффициентами

## Формулировка

Для ОЛДУ $y'' + a_1 y' + a_0 y = 0$ с характеристическим уравнением $\lambda^2 + a_1 \lambda + a_0 = 0$:

| Случай | Корни | ФСР |
|--------|-------|-----|
| I) $\mathcal{D} > 0$ | $\lambda_1 \ne \lambda_2$ вещественные | $e^{\lambda_1 x},\ e^{\lambda_2 x}$ |
| II) $\mathcal{D} = 0$ | $\lambda_1 = \lambda_2 = \lambda_0$ вещественные | $e^{\lambda_0 x},\ x e^{\lambda_0 x}$ |
| III) $\mathcal{D} < 0$ | $\lambda = \alpha \pm \beta i$ комплексные | $e^{\alpha x}\cos\beta x,\ e^{\alpha x}\sin\beta x$ |

## Доказательство

**I)** $y = e^{\lambda_i x}$: $y' = \lambda_i e^{\lambda_i x}$, $y'' = \lambda_i^2 e^{\lambda_i x}$. Подставляем: $(\lambda_i^2 + a_1 \lambda_i + a_0)e^{\lambda_i x} = 0$ (так как $\lambda_i$ — корень ХУ).
Вронскиан: $W = (\lambda_2 - \lambda_1)e^{(\lambda_1+\lambda_2)x} \ne 0 \implies$ ФСР.

**II)** $y = e^{\lambda_0 x}$ — решение (аналогично I). Проверим $y = x e^{\lambda_0 x}$:
$$
y' = e^{\lambda_0 x} + \lambda_0 x e^{\lambda_0 x},\quad y'' = 2\lambda_0 e^{\lambda_0 x} + \lambda_0^2 x e^{\lambda_0 x}
$$
Подставляя в $y'' - 2\lambda_0 y' + \lambda_0^2 y = 0$ (так как $(\lambda-\lambda_0)^2 = \lambda^2 - 2\lambda_0\lambda + \lambda_0^2$), получаем $0$.
$W[e^{\lambda_0 x}, x e^{\lambda_0 x}] = e^{2\lambda_0 x} \ne 0 \implies$ ФСР.

**III)** $e^{\lambda_1 x} = e^{\alpha x}(\cos\beta x + i\sin\beta x)$. Тогда:
$$
\operatorname{Re}(e^{\lambda_1 x}) = e^{\alpha x}\cos\beta x = \frac{1}{2}e^{\lambda_1 x} + \frac{1}{2}e^{\lambda_2 x}
$$
$$
\operatorname{Im}(e^{\lambda_1 x}) = e^{\alpha x}\sin\beta x = \frac{1}{2i}e^{\lambda_1 x} - \frac{1}{2i}e^{\lambda_2 x}
$$
Обе функции — ЛК решений $e^{\lambda_1 x}, e^{\lambda_2 x}$, значит тоже решения. Определитель перехода $\begin{vmatrix} 1/2 & 1/(2i) \\ 1/2 & -1/(2i) \end{vmatrix} = i/2 \ne 0$, значит они ЛНС. $\blacksquare$

---

# Билет 22. Метод вариации постоянных для НЛДУ

## Формулировка

**Теорема (метод вариации постоянных).** Пусть $y_1(x), y_2(x)$ — ФСР ОЛДУ $y'' + a_1(x)y' + a_0(x)y = 0$. Тогда общее решение НЛДУ $y'' + a_1(x)y' + a_0(x)y = f(x)$ может быть представлено в виде:
$$
y_{\text{о.н.}} = C_1(x) \cdot y_1(x) + C_2(x) \cdot y_2(x)
$$
где $C_1(x), C_2(x)$ — всевозможные решения системы:
$$
\begin{cases} y_1(x) \cdot C_1' + y_2(x) \cdot C_2' = 0 \\ y_1'(x) \cdot C_1' + y_2'(x) \cdot C_2' = f(x) \end{cases}
$$

## Доказательство

Система — крамеровская ($W \ne 0$), $\exists! (C_1', C_2')$:
$$
C_1' = \frac{\Delta_1(x)}{W(x)} = \psi_1(x),\quad C_2' = \frac{\Delta_2(x)}{W(x)} = \psi_2(x)
$$
Интегрируем: $C_i(x) = \int \psi_i(x)\,dx + \mathcal{D}_i$. Тогда:
$$
C_1 y_1 + C_2 y_2 = \underbrace{\mathcal{D}_1 y_1 + \mathcal{D}_2 y_2}_{y_{\text{о.о.}}} + \underbrace{y_1 \int \psi_1\,dx + y_2 \int \psi_2\,dx}_{y_{\text{ч.н.}}}
$$

Подставляя $y_{\text{ч.н.}}$ в левую часть НЛДУ и используя систему для $\psi_1,\psi_2$, после дифференцирования и группировки получаем $f(x)$. $\blacksquare$

---

# Билет 23. Принцип суперпозиции для СЛДУ. Структура общего решения СЛНДУ

## Формулировка

СЛДУ в нормальной форме: $\dot{X} = A(t) X + F(t)$.

**Теорема 1 (принцип суперпозиции).** Если $X_1(t)$ — решение при $F=F_1$, $X_2(t)$ — при $F=F_2$, то $\forall \alpha_1,\alpha_2$: $X = \alpha_1 X_1 + \alpha_2 X_2$ — решение при $F = \alpha_1 F_1 + \alpha_2 F_2$.

**Теорема 2 (структура общего решения СЛНДУ).** Пусть $X_1(t)$ — частное решение СЛНДУ. Тогда:
1) $\forall$ решения $X_0(t)$ СОЛДУ, $X = X_0 + X_1$ — решение СЛНДУ.
2) $\forall$ решения $X_2(t)$ СЛНДУ $\exists$ решение $X_0(t)$ СОЛДУ: $X_2 = X_0 + X_1$.

Общее решение: $X_{\text{о.н.}}(t) = X_{\text{о.о.}}(t) + X_{\text{ч.н.}}(t)$.

## Доказательство

**Теорема 1.**
$$
(\alpha_1 X_1 + \alpha_2 X_2)' = \alpha_1 \dot{X}_1 + \alpha_2 \dot{X}_2 = \alpha_1(A X_1 + F_1) + \alpha_2(A X_2 + F_2) = A(\alpha_1 X_1 + \alpha_2 X_2) + (\alpha_1 F_1 + \alpha_2 F_2)
$$

**Теорема 2.**
1) $(X_0 + X_1)' = \dot{X}_0 + \dot{X}_1 = A X_0 + A X_1 + F = A(X_0 + X_1) + F$.
2) $(X_2 - X_1)' = A X_2 + F - A X_1 - F = A(X_2 - X_1)$, значит $X_2 - X_1$ — решение СОЛДУ. $\blacksquare$

---

# Билет 24. Принцип суперпозиции для СОЛДУ. Достаточное условие ЛН решений СОЛДУ

## Формулировка

**Теорема 1 (принцип суперпозиции для СОЛДУ).** Если $X_1,\dots,X_n$ — решения СОЛДУ $\dot{X} = A(t) X$, то $\forall \alpha_1,\dots,\alpha_n$: $\alpha_1 X_1 + \dots + \alpha_n X_n$ — тоже решение.

**Теорема 2 (связь с вронскианом).** Если $X_1,\dots,X_n$ — решения СОЛДУ $n \times n$, то:
$$
X_1,\dots,X_n \text{ — ЛНС} \iff \forall t \in I: W[X_1,\dots,X_n](t) \ne 0
$$

## Доказательство

**Теорема 1.** Следует из принципа суперпозиции для СЛДУ при $F_1 \equiv \dots \equiv F_n \equiv 0$. $\blacksquare$

**Теорема 2 (для $2 \times 2$).**
$(\Rightarrow)$: от противного. Пусть $X_1,X_2$ ЛНС, но $\exists t_0: W(t_0)=0$. Тогда столбцы $W(t_0)$ ЛЗ: $\exists (\alpha_1,\alpha_2) \ne (0,0)$ с $\alpha_1 X_1(t_0) + \alpha_2 X_2(t_0) = 0$. Функция $Z(t) = \alpha_1 X_1(t) + \alpha_2 X_2(t)$ — решение СОЛДУ с нулевыми НУ, значит $Z(t) \equiv 0$, противоречие с ЛНС.

$(\Leftarrow)$: если бы $X_1,X_2$ были ЛЗ, то $W \equiv 0$. $\blacksquare$

---

# Билет 25. Существование ФСР у СОЛДУ. Структура общего решения СОЛДУ

## Формулировка

**Теорема 1 (о $\exists$ ФСР СОЛДУ).** У СОЛДУ $\dot{X} = A(t) X$ существует ФСР.

**Теорема 2 (структура общего решения СОЛДУ).** Пусть $X_1,\dots,X_n$ — ФСР СОЛДУ. Тогда $\forall$ решения $X_0(t)$ $\exists C_1,\dots,C_n$:
$$
X_0(t) = C_1 X_1(t) + \dots + C_n X_n(t),\quad t \in I
$$

Общее решение: $X_{\text{о.о.}}(t) = C_1 X_1(t) + \dots + C_n X_n(t)$.

## Доказательство

**Теорема 1 (для $2 \times 2$).** Решаем две задачи Коши:
$$
\begin{cases} \dot{X} = A(t) X \\ X_1(t_0) = (1,0)^T \end{cases} \quad
\begin{cases} \dot{X} = A(t) X \\ X_2(t_0) = (0,1)^T \end{cases}
$$
$W[X_1,X_2](t_0) = \begin{vmatrix} 1 & 0 \\ 0 & 1 \end{vmatrix} = 1 \ne 0 \implies$ ФСР. $\blacksquare$

**Теорема 2.** Для $t_0 \in I$ решаем СЛУ с матрицей $(X_1(t_0),\dots,X_n(t_0))$. Так как $W(t_0) \ne 0$, $\exists!$ набор $(C_1,\dots,C_n)$. Функция $\sum C_i X_i(t)$ — решение той же задачи Коши, что и $X_0(t)$. По единственности, они совпадают. $\blacksquare$

---

# Билет 26. ФСР СОЛДУ $2 \times 2$: случай некратных СЗ матрицы

## Формулировка

Для СОЛДУ $\dot{X} = A X$ с постоянными вещественными коэффициентами:

**1) Вещественные различные СЗ:** ФСР:
$$
X = X_1 e^{\lambda_1 t},\quad X = X_2 e^{\lambda_2 t}
$$
где $X_i$ — СВ-столбцы матрицы $A$ для $\lambda_i$.

**2) Комплексные СЗ ($\lambda = \alpha \pm \beta i$, $\beta \ne 0$):** ФСР:
$$
X = \operatorname{Re}(X_1 e^{\lambda_1 t}),\quad X = \operatorname{Im}(X_1 e^{\lambda_1 t})
$$
где $X_1$ — СВ-столбец для $\lambda_1$.

## Доказательство

**1)** $(X_i e^{\lambda_i t})' = \lambda_i X_i e^{\lambda_i t} = A X_i e^{\lambda_i t}$ (так как $A X_i = \lambda_i X_i$). Значит $X_i e^{\lambda_i t}$ — решение.
$$
W = e^{(\lambda_1+\lambda_2)t} \cdot \begin{vmatrix} x_{11} & x_{12} \\ x_{21} & x_{22} \end{vmatrix}
$$
Так как $X_1,X_2$ ЛНС (СВ для различных СЗ), определитель $\ne 0$, значит $W \ne 0$ — ФСР.

**2)** $X_1 e^{\lambda_1 t}$ и $X_2 e^{\lambda_2 t} = (X_1)^* e^{\overline{\lambda_1} t}$ — комплексная ФСР. Тогда:
$$
\operatorname{Re}(X_1 e^{\lambda_1 t}) = \frac{1}{2} X_1 e^{\lambda_1 t} + \frac{1}{2} X_2 e^{\lambda_2 t}
$$
$$
\operatorname{Im}(X_1 e^{\lambda_1 t}) = \frac{1}{2i} X_1 e^{\lambda_1 t} - \frac{1}{2i} X_2 e^{\lambda_2 t}
$$
Обе — ЛК решений, значит тоже решения. Определитель перехода $\ne 0$, значит они ЛНС. $\blacksquare$

---

# Билет 27. Метод вариации постоянных для СЛНДУ

## Формулировка

**Теорема (метод вариации постоянных для СЛНДУ).** Общее решение СЛНДУ $\dot{X} = A(t) X + F(t)$ можно представить в виде:
$$
X_{\text{о.н.}} = C_1(t) \cdot X_1(t) + C_2(t) \cdot X_2(t)
$$
где $X_1(t), X_2(t)$ — ФСР соответствующей СОЛДУ, а $(C_1(t), C_2(t))$ — всевозможные решения системы:
$$
\Phi(t) \cdot \dot{C} = F(t), \qquad \Phi(t) = (X_1(t), X_2(t))
$$

В матричной форме: $X_{\text{о.н.}}(t) = \Phi(t) C(t)$, где $\Phi(t) \dot{C}(t) = F(t)$.

## Доказательство

$|\Phi(t)| = W(t) \ne 0 \implies \exists \Phi^{-1}(t)$ (непрерывна). Тогда $\dot{C} = \Phi^{-1}(t) F(t)$. Интегрируем:
$$
C = D + \int \Phi^{-1}(t) F(t)\,dt
$$
Подставляем $X = \Phi(t) C(t) = \underbrace{\Phi(t) D}_{X_{\text{о.о.}}} + \underbrace{\Phi(t) \int \Phi^{-1}(t) F(t)\,dt}_{X_{\text{ч.н.}}}$.

Проверка: $(\Phi(t) \int \Phi^{-1} F\,dt)' = \dot{\Phi} \int \Phi^{-1} F\,dt + \Phi \cdot \Phi^{-1} F = A \Phi \int \Phi^{-1} F\,dt + F = A X_{\text{ч.н.}} + F$. Значит $X_{\text{ч.н.}}$ — частное решение СЛНДУ. $\blacksquare$
